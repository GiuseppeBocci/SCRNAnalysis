% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/normalization.R
\name{normalizeCPM}
\alias{normalizeCPM}
\alias{normalizeCPM,ScDeconvExperiment-method}
\title{Normalize counts to CPM}
\usage{
normalizeCPM(x)

\S4method{normalizeCPM}{ScDeconvExperiment}(x)
}
\arguments{
\item{x}{A ScDeconvExperiment object.}
}
\value{
A ScDeconvExperiment object with CPM values stored in
the \code{"normcounts"} assay.
}
\description{
Normalizes raw counts by dividing each cell by its library size
and multiplying by 1,000,000.
}
\examples{
set.seed(1)
counts <- matrix(
  rpois(1000, lambda = 5),
  nrow = 100,
  ncol = 10
)
rownames(counts) <- paste0("Gene", seq_len(nrow(counts)))
colnames(counts) <- paste0("Cell", seq_len(ncol(counts)))

sce <- SingleCellExperiment::SingleCellExperiment(
  assays = list(counts = counts)
)

se <- ScDeconvExperiment(sce)

normalized_se <- normalizeCPM(se)

SummarizedExperiment::assayNames(normalized_se)

}
