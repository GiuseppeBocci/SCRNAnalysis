% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/assesment.R
\name{filterCellsByDetectedGenes}
\alias{filterCellsByDetectedGenes}
\alias{filterCellsByDetectedGenes,ScDeconvExperiment-method}
\title{Filter low-quality cells by detected genes}
\usage{
filterCellsByDetectedGenes(
  object,
  assay_name = "counts",
  min_count = 1,
  lower_percentile = 0.01,
  upper_percentile = 0.99,
  store_metrics = TRUE
)

\S4method{filterCellsByDetectedGenes}{ScDeconvExperiment}(
  object,
  assay_name = "counts",
  min_count = 1,
  lower_percentile = 0.01,
  upper_percentile = 0.99,
  store_metrics = TRUE
)
}
\arguments{
\item{object}{A ScDeconvExperiment object.}

\item{assay_name}{Name of the assay to use. Default is "counts".}

\item{min_count}{Minimum count required to consider a gene expressed.}

\item{lower_percentile}{Lower percentile for detected genes per cell.}

\item{upper_percentile}{Upper percentile for detected genes per cell.}

\item{store_metrics}{Logical. Whether to store cell-level metrics in colData.}
}
\value{
The filtered ScDeconvExperiment object.
}
\description{
Removes cells with too few or too many detected genes using
percentile-based thresholds.
}
\examples{
set.seed(1)
counts <- matrix(
  rpois(1000, lambda = 5),
  nrow = 100,
  ncol = 10
)
rownames(counts) <- paste0("Gene", seq_len(nrow(counts)))
colnames(counts) <- paste0("Cell", seq_len(ncol(counts)))

sce <- SingleCellExperiment::SingleCellExperiment(
  assays = list(counts = counts)
)

se <- ScDeconvExperiment(sce)

filtered_se <- filterCellsByDetectedGenes(
  se,
  min_count = 1,
  lower_percentile = 0.01,
  upper_percentile = 0.99
)

filtered_se@params$filterCellsByDetectedGenes

}
