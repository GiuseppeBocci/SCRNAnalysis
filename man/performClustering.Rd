% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis.R
\name{performClustering}
\alias{performClustering}
\alias{performClustering,ScDeconvExperiment-method}
\title{Perform clustering of cells}
\usage{
performClustering(
  object,
  assay_name = "logcounts",
  algorithm = c("louvain", "leiden", "kmeans", "hierarchical"),
  use_hvg = TRUE,
  hvg_column = "is_hvg",
  hvg_rank_column = "hvg_rank",
  n_hvg = NULL,
  n_pcs = 20,
  n_clusters = 10,
  k = 20,
  resolution = 1,
  cluster_column = "cluster",
  pca_name = "PCA"
)

\S4method{performClustering}{ScDeconvExperiment}(
  object,
  assay_name = "logcounts",
  algorithm = c("louvain", "leiden", "kmeans", "hierarchical"),
  use_hvg = TRUE,
  hvg_column = "is_hvg",
  hvg_rank_column = "hvg_rank",
  n_hvg = NULL,
  n_pcs = 20,
  n_clusters = 10,
  k = 20,
  resolution = 1,
  cluster_column = "cluster",
  pca_name = "PCA"
)
}
\arguments{
\item{object}{A ScDeconvExperiment object.}

\item{assay_name}{Name of the assay to use. Default is \code{"logcounts"}.}

\item{algorithm}{Clustering algorithm. One of \code{"louvain"}, \code{"leiden"},
\code{"kmeans"}, or \code{"hierarchical"}.}

\item{use_hvg}{Logical. Whether to use highly variable genes.}

\item{hvg_column}{Name of the rowData column marking HVGs.}

\item{hvg_rank_column}{Name of the rowData column containing HVG ranks.}

\item{n_hvg}{Optional number of HVGs to use.}

\item{n_pcs}{Number of principal components.}

\item{n_clusters}{Number of clusters for k-means and hierarchical clustering.}

\item{k}{Number of nearest neighbors for graph-based clustering.}

\item{resolution}{Resolution parameter for Louvain or Leiden clustering.}

\item{cluster_column}{Name of the colData column where clusters are stored.}

\item{pca_name}{Name of the reducedDim slot where PCA coordinates are stored.}
}
\value{
The ScDeconvExperiment object with cluster labels stored in colData.
}
\description{
Performs cell clustering using PCA followed by hierarchical, k-means,
Louvain, or Leiden clustering.
}
\examples{
if (requireNamespace("scater", quietly = TRUE)) {
  set.seed(1)
  counts <- matrix(
    rpois(1000, lambda = 5),
    nrow = 100,
    ncol = 10
  )
  rownames(counts) <- paste0("Gene", seq_len(nrow(counts)))
  colnames(counts) <- paste0("Cell", seq_len(ncol(counts)))
  
  logcounts <- log1p(counts)
  
  sce <- SingleCellExperiment::SingleCellExperiment(
    assays = list(
      counts = counts,
      logcounts = logcounts
    ),
    colData = S4Vectors::DataFrame(
      cell_type = rep(c("A", "B"), each = 5)
    ),
    rowData = S4Vectors::DataFrame(
      gene_id = rownames(counts)
    )
  )
  
  se <- ScDeconvExperiment(sce)

  se <- performClustering(
    se,
    assay_name = "logcounts",
    algorithm = "kmeans",
    use_hvg = FALSE,
    n_pcs = 5,
    n_clusters = 2
  )

  head(SummarizedExperiment::colData(se)$cluster)
}

}
