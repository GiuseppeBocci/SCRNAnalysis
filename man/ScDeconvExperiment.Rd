% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ScDeconvExperiment.R
\name{ScDeconvExperiment}
\alias{ScDeconvExperiment}
\title{Create a ScDeconvExperiment object}
\usage{
ScDeconvExperiment(sce, params = list(), history = character())
}
\arguments{
\item{sce}{A SingleCellExperiment object containing a \code{"counts"} assay.}

\item{params}{A list of analysis parameters. Defaults to an empty list.}

\item{history}{A character vector describing the analysis steps already
performed. Defaults to an empty character vector.}
}
\value{
A ScDeconvExperiment object.
}
\description{
Creates a ScDeconvExperiment object from a SingleCellExperiment object.
The input object must contain a \code{"counts"} assay.
}
\examples{
counts <- matrix(
  c(1, 0, 3, 4, 2, 0),
  nrow = 3,
  ncol = 2
)
rownames(counts) <- c("GeneA", "GeneB", "GeneC")
colnames(counts) <- c("Cell1", "Cell2")

sce <- SingleCellExperiment::SingleCellExperiment(
  assays = list(counts = counts)
)

obj <- ScDeconvExperiment(sce)
obj

}
