% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/normalization.R
\name{logTransform}
\alias{logTransform}
\alias{logTransform,ScDeconvExperiment-method}
\title{Log-transform normalized counts}
\usage{
logTransform(x, assay_name = "normcounts", pseudo_count = 1, base = 2)

\S4method{logTransform}{ScDeconvExperiment}(x, assay_name = "normcounts", pseudo_count = 1, base = 2)
}
\arguments{
\item{x}{A ScDeconvExperiment object.}

\item{assay_name}{Name of the assay to transform. Defaults to \code{"normcounts"}.}

\item{pseudo_count}{Numeric value added before log transformation.
Defaults to \code{1}.}

\item{base}{Logarithm base. Defaults to \code{2}.}
}
\value{
A ScDeconvExperiment object with log-transformed values stored in
the \code{"logcounts"} assay.
}
\description{
Applies a log transformation to an assay and stores the result
in the \code{"logcounts"} assay.
}
\examples{
set.seed(1)
counts <- matrix(
  rpois(1000, lambda = 5),
  nrow = 100,
  ncol = 10
)
rownames(counts) <- paste0("Gene", seq_len(nrow(counts)))
colnames(counts) <- paste0("Cell", seq_len(ncol(counts)))

normcounts <- t(t(counts) / colSums(counts)) * 1e4

sce <- SingleCellExperiment::SingleCellExperiment(
  assays = list(
    counts = counts,
    normcounts = normcounts
  )
)

se <- ScDeconvExperiment(sce)

log_transformed_se <- logTransform(
  se,
  assay_name = "normcounts",
  pseudo_count = 1,
  base = 2
)

SummarizedExperiment::assayNames(log_transformed_se)

}
