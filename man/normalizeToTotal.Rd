% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/normalization.R
\name{normalizeToTotal}
\alias{normalizeToTotal}
\alias{normalizeToTotal,ScDeconvExperiment-method}
\title{Normalize counts to a user-defined total}
\usage{
normalizeToTotal(x, target_total = 1e+06)

\S4method{normalizeToTotal}{ScDeconvExperiment}(x, target_total = 1e+06)
}
\arguments{
\item{x}{A ScDeconvExperiment object.}

\item{target_total}{Numeric scaling factor. Defaults to \code{1e6},
corresponding to CPM normalization.}
}
\value{
A ScDeconvExperiment object with normalized values stored in
the \code{"normcounts"} assay.
}
\description{
Normalizes raw counts by dividing each cell by its library size
and multiplying by a user-defined target total.
}
\examples{
set.seed(1)
counts <- matrix(
  rpois(1000, lambda = 5),
  nrow = 100,
  ncol = 10
)
rownames(counts) <- paste0("Gene", seq_len(nrow(counts)))
colnames(counts) <- paste0("Cell", seq_len(ncol(counts)))

sce <- SingleCellExperiment::SingleCellExperiment(
  assays = list(counts = counts)
)

se <- ScDeconvExperiment(sce)

normalized_se <- normalizeToTotal(
  se,
  target_total = 1e4
)

SummarizedExperiment::assayNames(normalized_se)

}
