% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/assesment.R
\name{filterLowExpressionGenes}
\alias{filterLowExpressionGenes}
\alias{filterLowExpressionGenes,ScDeconvExperiment-method}
\title{Filter genes with very low expression}
\usage{
filterLowExpressionGenes(
  object,
  assay_name = "counts",
  min_count = 1,
  lower_percentile = 0.01,
  min_cells = NULL,
  store_metrics = TRUE
)

\S4method{filterLowExpressionGenes}{ScDeconvExperiment}(
  object,
  assay_name = "counts",
  min_count = 1,
  lower_percentile = 0.01,
  min_cells = NULL,
  store_metrics = TRUE
)
}
\arguments{
\item{object}{A ScDeconvExperiment object.}

\item{assay_name}{Name of the assay to use. Default is "counts".}

\item{min_count}{Minimum count required to consider a gene expressed
in a cell.}

\item{lower_percentile}{Percentile used to define the minimum number
of cells in which a gene should be detected.}

\item{min_cells}{Optional absolute minimum number of cells. If NULL,
it is estimated from lower_percentile.}

\item{store_metrics}{Logical. Whether to store gene-level metrics in rowData.}
}
\value{
The filtered ScDeconvExperiment object.
}
\description{
Removes genes expressed in too few cells.
}
\examples{
set.seed(1)
counts <- matrix(
  rpois(1000, lambda = 5),
  nrow = 100,
  ncol = 10
)
rownames(counts) <- paste0("Gene", seq_len(nrow(counts)))
colnames(counts) <- paste0("Cell", seq_len(ncol(counts)))

sce <- SingleCellExperiment::SingleCellExperiment(
  assays = list(counts = counts)
)

se <- ScDeconvExperiment(sce)

filtered_se <- filterLowExpressionGenes(
  se,
  min_count = 1,
  lower_percentile = 0.01
)

filtered_se@params$filterLowExpressionGenes

}
