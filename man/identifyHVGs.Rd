% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis.R
\name{identifyHVGs}
\alias{identifyHVGs}
\alias{identifyHVGs,ScDeconvExperiment-method}
\title{Identify highly variable genes}
\usage{
identifyHVGs(
  object,
  assay_name = "logcounts",
  n_top = 2000,
  method = "dispersion",
  min_mean = 0,
  max_mean = Inf,
  store_metrics = TRUE
)

\S4method{identifyHVGs}{ScDeconvExperiment}(
  object,
  assay_name = "logcounts",
  n_top = 2000,
  method = "dispersion",
  min_mean = 0,
  max_mean = Inf,
  store_metrics = TRUE
)
}
\arguments{
\item{object}{A ScDeconvExperiment object.}

\item{assay_name}{Name of the assay to use. Default is "logcounts".}

\item{n_top}{Number of highly variable genes to select.}

\item{method}{Variability score to use. One of "variance", "dispersion", or "cv2".}

\item{min_mean}{Minimum average expression required for a gene to be eligible.}

\item{max_mean}{Maximum average expression allowed for a gene to be eligible.}

\item{store_metrics}{Logical. Whether to store HVG metrics in rowData.}
}
\value{
The ScDeconvExperiment object with HVG information stored in rowData.
}
\description{
Identifies highly variable genes using gene-level variability statistics.
}
\examples{
set.seed(1)
counts <- matrix(
  rpois(1000, lambda = 5),
  nrow = 100,
  ncol = 10
)
rownames(counts) <- paste0("Gene", seq_len(nrow(counts)))
colnames(counts) <- paste0("Cell", seq_len(ncol(counts)))

logcounts <- log1p(counts)

sce <- SingleCellExperiment::SingleCellExperiment(
  assays = list(
    counts = counts,
    logcounts = logcounts
  ),
  colData = S4Vectors::DataFrame(
    cell_type = rep(c("A", "B"), each = 5)
  ),
  rowData = S4Vectors::DataFrame(
    gene_id = rownames(counts)
  )
)

se <- ScDeconvExperiment(sce)

se <- identifyHVGs(
  se,
  assay_name = "logcounts",
  n_top = 20,
  method = "dispersion"
)

head(SummarizedExperiment::rowData(se)$is_hvg)
head(SummarizedExperiment::rowData(se)$hvg_rank)
head(SummarizedExperiment::rowData(se)$hvg_score)

}
